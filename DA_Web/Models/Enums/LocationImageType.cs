namespace DA_Web.Models.Enums
{
    public enum LocationImageType
    {
        @default, // 'default' is a keyword, so use '@'
        introduction,
        architecture,
        experience,
        cuisine
    }
}